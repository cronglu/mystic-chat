# 📜 江湖秘籍：mystic-chat 极客实战与使用技巧手册

本手册汇总了 `mystic-chat` 在局域网实际协作、多网段穿透、防偷窥抓包及紧急避险中的高阶使用技巧与最佳实践。

---

## 🎯 技巧一：闪电避险与“假死脱身”（防突击查岗）

在办公区或公开场合，难免遇到路过窥屏或突击检查，熟练运用以下热键组合可实现秒级脱身：

1. **瞬发焚毁（天火焚书）**：
   - 快捷键：`Ctrl + L`
   - **效果**：不仅清空界面视口，底层物理内存（RAM）中的密信切片会在 1 毫秒内执行全量零值覆写（Secure Zero-Wipe）。即便随后被查验终端，内存中也只有清零字节。
2. **紧急避世（安全退出）**：
   - 快捷键：`Esc` 或 `Ctrl + C`
   - **效果**：执行全内存抹除后安全退出终端，终端恢复干净提示符。
3. **终端历史隐匿（极客卫生习惯）**：
   - 启动前在 bash/zsh 命令前加一个空格（如果启用了 `HISTCONTROL=ignorespace`），启动命令不会被写入 `~/.bash_history` 或 `~/.zsh_history`：
     ```bash
      ./mystic-chat -room 秘密暗号
     ```
   - 退出后快速清屏：`clear` 或 `reset`。

---

## 🎭 技巧二：易容术与节奏掌控（身份伪装）

1. **多重身份切换**：
   - 快捷键：`Ctrl + R` 或输入 `/reroll`
   - **实战意义**：在讨论不同议题或转换角色时，随时重掷武侠门派与名宿。所有在线同门会看到系统公告：
     ```text
     ⚙️ 【天机阁】侠客施展易容秘术，化身为 【桃花岛·黄药师】
     ```
   - 配合昵称哈希专属主色，快速建立新的发言辨识度。
2. **动态调整即焚倒计时节奏**：
   - 快捷键：`Tab` 或 `F2`（15s / 30s / 60s / 120s 循环）
   - **闲聊阶段**：切到 `120s` 或 `60s`，给同门从容阅读的时间；
   - **敏感敏感凭据/账号/指令传达**：随手按两下 `Tab` 切到 `15s`，发出后 15 秒内字符迅速变暗、风化瓦解为残卷粒子（`·*~°`）并从全网内存彻底蒸发。

---

## 🐧 技巧三：Linux 服务器作为常驻“烽火台”（免装与跨子网技巧）

如果你所在的办公室有台常开的 Linux 服务器，可以将其打造为永不掉线的局域网中枢。

### 1. 使用 systemd 将 mystic-chat 配置为系统常驻烽火台（禁用 UDP）
在 Linux 服务器上创建服务文件 `/etc/systemd/system/mystic-chat.service`：
```ini
[Unit]
Description=Mystic-Chat JiangHu Relay Beacon
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mystic-chat
ExecStart=/opt/mystic-chat/mystic-chat -bootstrap -p2p-port 19001 -no-udp -ssh-port 2222 -admin-key MySecretKey2026
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```
启动并设为开机自启：
```bash
sudo systemctl daemon-reload
sudo systemctl enable --now mystic-chat
```

### 2. 跨办公室/跨子网接入方案（纯 TCP 网格）
如果部分同事在 `192.168.1.x`，另一部分在 `192.168.2.x`（网段被路由器隔离，UDP 无法互通）：
- 只要所有人启动时指定 Linux 引导节点：
  ```bash
  ./mystic-chat -peer 192.168.1.200:19001 -no-udp
  ```
- 节点接入后，PEX（对等节点交换协议）会自动打通跨子网对等数据通道。

### 3. 同事“免安装”极速连入
当其他同事电脑上既没有编译好的二进制，也没装 Go 环境时，只需让他们打开终端运行一行：
```bash
ssh <服务器IP> -p 2222
```
SSH 协议会自动分配一个独立的武侠身份与 PTY 交互会话，直接进入全功能终端！

---

## 🛡️ 技巧四：防路由器/网关抓包验证与密令房间

企业网关、智能交换机或透明代理常常会对内网流量进行深度包检测（DPI）。

### 1. 为什么无需担心网关 Wireshark 抓包？
- `mystic-chat` 的底层 TCP 握手时在内存中动态生成自签名证书，所有 P2P 报文均运行在 **TLS 1.3 强加密隧道** 之上。
- **验证方法**：在路由器或同网段机器打开 Wireshark，过滤端口 `19001`，你抓到的所有数据包均显示为 `Application Data (TLSv1.3)`，没有任何可识别的明文，也无法看出是聊天软件。

### 2. 多重密令房间（AES-256-GCM 隔离）
若局域网内有多个小团队，可随时开启私密暗号房间：
- 命令行启动：
  ```bash
  ./mystic-chat -room 聚贤庄夜宴
  ```
- 界面内动态切换：
  ```text
  /room 黑木崖密道
  ```
- **效果**：即使数据包被其他节点中继转发，由于缺少相同暗号派生出的 AES 密钥，其他节点收到后只会触发解密校验失败并静默丢弃，绝对无法偷窥内容。
- **退回公开大厅**：输入 `/room`（不带参数）即可退回公开大厅。

---

## 🚨 技巧五：管理员紧急熔断与闭谷演练（风险管控）

在应对失控或敏感合规场景时，管理员可以通过一道令旗瞬间销毁全网所有活跃节点的痕迹。

### 演练步骤：
1. **启动时约定管理口令**：
   ```bash
   ./mystic-chat -admin-key martial-arts-god-mode
   ```
2. **执行全网关服指令**：
   在任意节点的聊天输入框中输入：
   ```text
   /killswitch martial-arts-god-mode
   ```
3. **瞬间生效效果**：
   - 发送节点广播经 SHA-256 签名的闭谷令旗包。
   - 所有直连与级联节点接收到合规令旗后，**立即对本地内存执行 Secure Zero-Wipe**；
   - 终端全屏覆写为红色高危告警框：
     ```text
     ⚠️  【天绝闭谷令 · 全网紧急熔断】  ⚠️
     掌门发动天绝闭谷令，全网信件已被天火焚毁
     所有驻留内存已执行物理零值覆写 (Secure Zero-Wipe)，无任何痕迹残留。
     ```
   - 节点阻断所有新消息输入，按任意键即可退出程序。

---

## 💻 技巧六：常用密令与快捷键速查表

| 操作 | 方式 | 典型应用场景 |
| :--- | :--- | :--- |
| **施展易容术** | `Ctrl + R` 或 `/reroll` | 换个江湖马甲继续发言 |
| **切换即焚时限** | `Tab` 或 `F2` | 快速在 15s / 30s / 60s / 120s 间循环 |
| **自定义精确时限** | `/ttl 45s` 或 `/ttl 5m` | 针对特定密信设置定制寿命 |
| **天火焚书** | `Ctrl + L` 或 `/clear` | 秒级清空视口并零值覆写内存 |
| **切换加密暗号** | `/room <暗号>` | 切换独立 AES-256 加密房间 |
| **穿透连接指定节点**| `/connect <IP:端口>` | 局域网无 UDP 时的手动握手连通 |
| **巡视同门** | `/peers` | 实时打印当前直连的其他侠客与地址 |
| **紧急闭谷熔断** | `/killswitch <管理口令>` | 全网瞬间覆写内存并关停退出 |
| **避世退出** | `Esc` 或 `Ctrl + C` | 内存物理抹除后安全退出终端 |
| **查看秘籍帮助** | `/help` | 调出终端内嵌指令速查清单 |

---

## 🪟 技巧七：同一台电脑多开实例测试（智能端口避让）

如果你在本地开发或自测时，希望在**同一台电脑打开 2 个甚至多个终端**互相发信密聊：

1. **全自动端口冲突避让（最新版本已支持）**：
   - 第 1 个终端运行：
     ```bash
     ./mystic-chat -peer 10.20.13.115
     ```
     *默认禁用 UDP，自动连接 19001 端口，占用本地 `:19001` 端口与 `:2222` SSH 端口。*
   - 第 2 个、第 3 个终端直接运行**同一条极简命令**：
     ```bash
     ./mystic-chat -peer 10.20.13.115
     ```
     *客户端检测到本地 `:19001` 已被第 1 个实例占用时，会**自动向操作系统申请随机空闲端口（Port 0 机制）**，实现零配置同机多开！*

2. **手动指定端口多开（推荐明确管理）**：
   - 实例 1：
     ```bash
     ./mystic-chat -peer 10.20.13.115 -p2p-port 19001
     ```
   - 实例 2：
     ```bash
     ./mystic-chat -peer 10.20.13.115 -p2p-port 0 -no-ssh
     ```
     *`-p2p-port 0` 让操作系统自动分配未被占用的端口，`-no-ssh` 避免本地抢占 2222 SSH 端口。*

